Basic Rust tictactoe program; Only a exercise learning a new language.
It plays using minimax with alpha-beta prunning. It never loses.
Beware the weird user interface, I really wasn't interested in make it user-friendly for the moment.
Build it and run with the basic Cargo commands.
